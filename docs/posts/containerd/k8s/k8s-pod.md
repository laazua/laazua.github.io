##### pod


<pre>
  1. pod是一种逻辑上的概念,
     它的的逻辑可以归纳为多个容器共享: network,IPC,PID.

  2. 具体实现上,一个pod至少包含一个pause的容器,
     它负责对整个pod进行初始化和僵尸进程的回收工作.
</pre>

- **启动探测**

- **就绪探测**
    + pod的整个生命周期内都在检测

- **存活探测**