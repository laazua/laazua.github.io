### 一些问题

- **缓存击穿**
> `查询一个存在但缓存过期的热点数据`. 大量访问直接访问MySQL(同一个key的大量访问)  
> 解决方案: 互斥锁, 逻辑过期(热点数据永不过期)  
---
- **缓存穿透**
> `查询一个不存在的数据(缓存和数据库都没有)`. 同时有大量访问访问系统(同一个key的大量访问)  
> 解决方案: 布隆过滤器, 缓存空对象  
---
- **缓存雪崩**
> `大量缓存key在同一时间过期失效`. 所有请求都直接打到数据库，造成数据库瞬间压力过大甚至宕机
> 解决方案: 过期时间随机化  
